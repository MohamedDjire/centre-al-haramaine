<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Ne pas envoyer l'API / uploads vers index.html (React)
  RewriteRule ^backend/ - [L]
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
